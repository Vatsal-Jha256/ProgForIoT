# FedRoute Hardware Demo - Standalone Requirements
# This demo is independent and does not require the parent repo

# Hardware control (for Raspberry Pi)
RPi.GPIO>=0.7.0
adafruit-circuitpython-ssd1306>=2.12.0
Pillow>=9.0.0

# Basic dependencies
numpy>=1.21.0

# Note: For simulation mode, only numpy is required
# Hardware packages are optional and only needed on Raspberry Pi
